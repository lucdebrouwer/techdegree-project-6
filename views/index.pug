extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div
      h1 Portfolio | Luc de Brouwer
      p.lead.text-light
        | Writing JavaScript is one of my favorite things to do. What started with watching videos about JavaScript eventually turned into creating projects with JavaScript. In my portfolio you will find unique JavaScript projects that are part of the fullstack JavaScript techdegree from Treehouse.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      each project in projects
       .cell
        a(href=`/project/${project.id}`)
         img.thumbnail(src=`${project.image_urls[0]}`)
         h5 #{ project.project_name }
      
